AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Serverless E-commerce Orders Service

Globals:
  Function:
    Timeout: 30
    Runtime: nodejs20.x
    MemorySize: 512
    Architectures:
      - x86_64
    Environment:
      Variables:
        NODE_ENV: !Ref Environment
        AWS_NODEJS_CONNECTION_REUSE_ENABLED: 1
        ORDERS_TABLE_NAME: !Ref OrdersTable
        EVENT_BUS_NAME: dev-orders-event-bus

    Tracing: Active

  Api:
    Cors:
      AllowMethods: "'GET,POST,PUT,DELETE,OPTIONS'"
      AllowHeaders: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key'"
      AllowOrigin: "'*'"

Parameters:
  Environment:
    Type: String
    Default: dev
    AllowedValues:
      - dev
      - staging
      - prod

Resources:
  OrdersApi:
    Type: AWS::Serverless::Api
    Properties:
      Name: !Sub '${Environment}-orders-api'
      StageName: !Ref Environment
      TracingEnabled: true
      Auth:
        DefaultAuthorizer: CognitoAuthorizer
        Authorizers:
          CognitoAuthorizer:
            UserPoolArn:
              Fn::ImportValue: !Sub '${Environment}-UserPoolArn'

  OrdersTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub '${Environment}-orders'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: orderId
          AttributeType: S
        - AttributeName: userId
          AttributeType: S
        - AttributeName: createdAt
          AttributeType: S
      KeySchema:
        - AttributeName: orderId
          KeyType: HASH
      GlobalSecondaryIndexes:
        - IndexName: UserOrdersIndex
          KeySchema:
            - AttributeName: userId
              KeyType: HASH
            - AttributeName: createdAt
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
      Tags:
        - Key: Environment
          Value: !Ref Environment

  OrderHistoryTable:
    Type: AWS::DynamoDB::Table 
    Properties:
      TableName: !Sub '${Environment}-order-history'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: orderId
          AttributeType: S
        - AttributeName: timestamp
          AttributeType: S
      KeySchema:
        - AttributeName: orderId
          KeyType: HASH
        - AttributeName: timestamp
          KeyType: RANGE
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: orders

  IdempotencyTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub '${Environment}-idempotency-keys'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: idempotencyKey
          AttributeType: S
      KeySchema:
        - AttributeName: idempotencyKey
          KeyType: HASH
      TimeToLiveSpecification:
        AttributeName: expiresAt # <- DynamoDB has a features called Time To Live that automatically deletes old items for you
        Enabled: true
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: orders



  CreateOrderFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${Environment}-create-order'
      CodeUri: .
      Handler: dist/handlers/http/createOrder.handler
      Environment:
        Variables:
          IDEMPOTENCY_TABLE_NAME: !Ref IdempotencyTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref OrdersTable
        - DynamoDBCrudPolicy:
            TableName: !Ref IdempotencyTable
        # Add Permission for this function to can send evenPublisher 
        - Statement:
            - Effect: Allow 
              Action:
                - events:PutEvents
              Resource: !Sub 'arn:aws:events:${AWS::Region}:${AWS::AccountId}:event-bus/${Environment}-ecommerce-events'

            - Effect: Allow 
              Action:
                - cloudwatch:PutMetricData # for adding metric in code for adding more logs in cloudwatch
              Resource: '*'
      Events:
        CreateOrderApi:
          Type: Api
          Properties:
            RestApiId: !Ref OrdersApi
            Path: /orders
            Method: POST

  GetOrderFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${Environment}-get-order'
      CodeUri: .
      Handler: dist/handlers/http/getOrder.handler
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref OrdersTable
      Events:
        GetOrderApi:
          Type: Api
          Properties:
            RestApiId: !Ref OrdersApi
            Path: /orders/{orderId}
            Method: GET

  ListOrdersFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${Environment}-list-orders'
      CodeUri: .
      Handler: dist/handlers/http/listOrders.handler
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref OrdersTable
      Events:
        ListOrdersApi:
          Type: Api
          Properties:
            RestApiId: !Ref OrdersApi
            Path: /orders
            Method: GET

  UpdateOrderStatusFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${Environment}-update-order-status'
      CodeUri: .
      Handler: dist/handlers/http/updateOrderStatus.handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref OrdersTable
        # Add Permission for this function to can send evenPublisher 
        - Statement:
            - Effect: Allow 
              Action:
                - events:PutEvents
              Resource: '*'
      Events:
        UpdateOrderStatusApi:
          Type: Api
          Properties:
            RestApiId: !Ref OrdersApi
            Path: /orders/{orderId}/status
            Method: PUT

  UpdateOrderItemsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${Environment}-update-order-items'
      CodeUri: .
      Handler: dist/handlers/http/updateOrderItems.handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref OrdersTable
      Events:
        UpdateItemsApi:
          Type: Api
          Properties:
            RestApiId: !Ref OrdersApi
            Path: /orders/{orderId}/items
            Method: PUT
  CancelOrderFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${Environment}-cancel-order'
      CodeUri: .
      Handler: dist/handlers/http/cancelOrder.handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref OrdersTable
        - Statement:
            - Effect: Allow
              Action:
                - events:PutEvents
              Resource: !Sub 'arn:aws:events:${AWS::Region}:${AWS::AccountId}:event-bus/${Environment}-ecommerce-events'
      Events:
        CancelOrderApi:
          Type: Api
          Properties:
            RestApiId: !Ref OrdersApi
            Path: /orders/{orderId}/cancel
            Method: POST

  DeleteOrderFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${Environment}-delete-order'
      CodeUri: .
      Handler: dist/handlers/http/deleteOrder.handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref OrdersTable
        # Add Permission for this function to can send evenPublisher 
        - Statement:
            - Effect: Allow 
              Action:
                - events:PutEvents
              Resource: '*'
      Events:
        DeleteOrderApi:
          Type: Api
          Properties:
            RestApiId: !Ref OrdersApi
            Path: /orders/{orderId}
            Method: DELETE
  
  GetOrderHistoryFunction:
    Type: AWS::Serverless::Function 
    Properties:
      FunctionName: !Sub '${Environment}-get-order-history'
      CodeUri: .
      Handler: dist/handlers/http/getOrderHistory.handler
      Environment:
        Variables:
          ORDER_HISTORY_TABLE_NAME: !Ref OrderHistoryTable
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref OrdersTable
        - DynamoDBReadPolicy:
            TableName: !Ref OrderHistoryTable
      Events:
        GetHistoryApi:
          Type: Api
          Properties:
            RestApiId: !Ref OrdersApi
            Path: /orders/{orderId}/history
            Method: GET

  
  

  CreateOrderLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/lambda/${Environment}-create-order'
      RetentionInDays: 7

  GetOrderLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/lambda/${Environment}-get-order'
      RetentionInDays: 7

  ListOrdersLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/lambda/${Environment}-list-orders'
      RetentionInDays: 7


  UpdateOrderStatusLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/lambda/${Environment}-update-order-status'
      RetentionInDays: 7

  DeleteOrderLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/lambda/${Environment}-delete-order'
      RetentionInDays: 7

Outputs:
  ApiEndpoint:
    Description: API Gateway endpoint URL
    Value: !Sub 'https://${OrdersApi}.execute-api.${AWS::Region}.amazonaws.com/${Environment}'

  OrdersTableName:
    Description: DynamoDB Orders table name
    Value: !Ref OrdersTable

  OrderHistoryTableName:
    Description: Order History DynamoDB Table Name
    Value: !Ref OrderHistoryTable
    Export:
      Name: !Sub '${Environment}-OrderHistoryTableName'

  UpdateOrderStatusFunctionArn:
    Description: Update Order Status Lambda Function ARN
    Value: !GetAtt UpdateOrderStatusFunction.Arn

  DeleteOrderFunctionArn:
    Description: Delete Order Lambda Function ARN
    Value: !GetAtt DeleteOrderFunction.Arn

  IdempotencyTableName:
    Description: Idempotency Keys Table Name
    Value: !Ref IdempotencyTable
    Export:
      Name: !Sub '${Environment}-IdempotencyTableName'